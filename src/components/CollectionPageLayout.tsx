// Deprecated: collection pages are now built dynamically at
// src/app/collections/[slug]/page.tsx. This file is intentionally unused
// and kept empty to avoid stale type errors against the current tokens.ts shape.
export default function CollectionPageLayout() {
  return null
}
